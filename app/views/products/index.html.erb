<p class="firstline" id="notice"><%= notice %></p>

<h1 >Listing Products</h1>


<!-- <div style="overflow-x:auto;">
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Image url</th>
        <th>Colour</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @products.each do |product| %>
        <tr>
          <td><%= product.name %></td>
          <td><%= product.description %></td>
          <td><%= product.image_url %></td>
          <td><%= product.colour %></td>
          <td><%= link_to 'Show', product %></td>
          <td><%= link_to 'Edit', edit_product_path(product) %></td>
          <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
 -->
  <div class="container-fluid">
    <div class="row">
      <% @products.each do |product| %>
      <div class="col-sm-2 col-md-4 product-box">
        <%= image_tag(product.image_url, class: "img-responsive product-image") %>
        <h1><%= product.name %></h1>
        <p><%= product.description %></p>
        <p><%= product.colour %></p>
        <p><strong>$ <%= product.price %></strong></p>
        <p class="options"><%= link_to 'Show', product %>
        <%= link_to 'Edit', edit_product_path(product) %>
        <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></p>
      </div><!-- /col -->
      <% end %>
    </div>
  </div>

<br>

<p class="options"><%= link_to 'New Product', new_product_path %></p>